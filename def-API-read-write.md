# 读写 API

## 基本定义

我们的系统建立在虚拟时间(Time)、事件(Event)和历史(History)的基础之上。
在这个系统中,每个节点可以存储多个 History 值。
一个`MonoHistory`包含了所有小于等于时间 T 的历史记录,这包括在这些时刻发生的所有 Event 及其依赖关系。

节点的基本读写接口定义如下:

```rust
impl Node {
	fn read(&self) -> Vec<MonoHistory>;
	fn write(&mut self, h: MonoHistory);
}
```

根据我们之前的假设，时间 T 是唯一的，不同进程不会产生相同的时间 T。
因此，不会出现两个 MonoHistory 具有相同的最大时间 T 但内容不同的情况。

write 函数的实现是记录每次写入的 History。
当写入一个新的 History h 时, 不会覆盖以前写入的 History. 系统会同时存储多个 History。如果写入的 History 和现有的 History 有 Prefix 关系, `h ⊑ h_i` 或 `h_i ⊑ h`, 则合并.

虽然在我们的常识中，后写入的数据会覆盖先写入的数据，那是因为我们默认时间是 totally ordered 的, 并且我们默认读操作是返回单结果的.
但在本文讨论的基于虚拟非线性时间的系统中，我们需要:

1. 放弃时间 totally ordered 的假设(我们已经用虚拟时间取代了现实时间)
2. 重新思考较大时间的 History 是否应该覆盖较小时间的 History 以及如何覆盖，这个问题我们将在后面详细讨论。

## 示例

让我们通过一个具体的例子来说明这些操作。
假设系统中的时间值关系为 `⊥→{1,2}→{3,4}`,
写入存储时, History 都会被同时保留. 读操作则返回所有结果.
以下是一系列读写操作的示例.

注意其中`⊥→1→3` 和 `⊥→2` 不 Compatible(即`⊥→1→3 ≭ ⊥→2`), 因为合并后的结果 `⊥→{1,2}→3` 中 T=3 的历史事件多出了一个 T=2 的依赖事件,
相当于改变了时刻 T=3 的历史.
同样也有 `⊥→1→3 ≭ ⊥→{1,2}→4` 和 `⊥→2→4 ≭ ⊥→{1,2}→4`.
但 `⊥→2 ≍ ⊥→2→4`, 所以被合并到 `⊥→2→4`.

```text
- write(⊥→2)
- read()              // → [⊥→2]

- write(⊥→1→3)        // ∵ ⊥→1→3 ≭ ⊥→2
                      // ∴ keep both
- read()              // → [⊥→1→3, ⊥→2]

- write(⊥→2→4)        // ∵ ⊥→2 ≍ ⊥→2→4
                      // ∴ merge them
- read()              // → [⊥→1→3, ⊥→2→4]

- write(⊥→{1,2}→4)    // ∵ ⊥→1→3 ≭ ⊥→{1,2}→4
                      // ∵ ⊥→2→4 ≭ ⊥→{1,2}→4
                      // ∴ keep all
- read()              // → [⊥→1→3, ⊥→2→4, ⊥→{1,2}→4]
```

![](history-12-34-rw.x.svg)
