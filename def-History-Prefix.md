我们定义 History 之间的前缀关系：对于两个 History h1 和 h2，
如果通过若干次添加新的 Event 节点以及添加从已有节点指向新节点的边，能够从 h1 得到 h2，
那么我们称 h1 是 h2 的前缀(Prefix)，记作 `h1 ⊑ h2`。
此时也可以说 h2 是 h1 的 successor，h1 是 h2 的 ancestor。

History 的Prefix关系表示历史的演进路径，即一个 History 是另一个 History 的某个过去状态。

History 的 Prefix 关系和图的子集关系相近但不一样.
下面通过例子说明 History 之间存在Prefix关系：
用数字 i 表示一个时间节点 Ei，考虑以下几个 History：`1->2`、`2->3`、`1->3` 和 `1->2->3, 1->3`：

- `1` 是 `1->2` 的前缀，因为可以通过添加 `->2` 得到 `1->2`
- `1` 是 `1->3` 的前缀，因为可以通过添加 `->3` 得到 `1->3`
- `2` 是 `2->3` 的前缀，因为可以通过添加 `->3` 得到 `2->3`
- `1->2` 是 `1->2->3` 的前缀，因为可以通过添加 `->3` 得到 `1->2->3`
- `1->2` 是 `1->{2,3}` 的前缀，因为可以通过添加 `->3` 得到 `1->{2,3}`
- `1->3` 是 `1->{2,3}` 的前缀，因为可以通过添加 `->2` 得到 `1->{2,3}`

以下是一些不构成Prefix关系的例子(虽然它们都是子图关系):
- `2` 不是 `1->2` 的前缀，因为无法构造出 `1->2` 的边
- `3` 不是 `1->3` 的前缀，因为无法构造出 `1->3` 的边
- `2->3` 不是 `1->2->3, 1->3` 的前缀，因为无法构造出 `1->{2,3}` 的边
- `1->3` 不是 `1->2->3, 1->3` 的前缀，因为无法构造出 `1->2->3` 的边
- `1->{2,3}` 不是 `1->2->3, 1->3` 的前缀，因为无法构造出 `2->3` 的边

![](history-prefix.x.svg)

根据 History 的定义，系统只允许上图中黑色箭头所示的 History 演进方式，即一个 History 只能变成它的 successor。

以 `E1->E3` 不是 `E1->E2->E3` 的前缀为例，可以这样理解：
在 `E1->E3` 中，E3 发生时不存在 E2，那么在后续状态中也不允许 E3 之前出现 E2，否则就违反了 E3 的因果关系。

举例说明：

```
E1：我出门了
E2：我买了一杯咖啡
E3：然后我回家了
```

对于 `E1->E3`，表示我空手回到家。此时无论发生什么，我手中都不会有咖啡。
如果允许下一个状态变为 `E1->E2->E3`，就相当于我手中凭空出现了一杯咖啡，这违反了因果关系。

参考：
- [Induced Subgraph](https://en.wikipedia.org/wiki/Induced_subgraph)