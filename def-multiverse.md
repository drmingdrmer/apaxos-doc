# 多重宇宙

这里的多重宇宙没有物理学里那么复杂:
平时我们将一个 copy 一个变量变成 2 个,每一个都独立的被使用, 那么就认为这个变量分裂为 2 个时间线了, 这 2 个时间线是平行的, 互不干扰的 History.

这 2 个互相平行的 History, 在某些阶段会再次合并, 一个替换另一个. 例如呈现为我们日常看到的多线程程序中的一些行为.
这些多重宇宙的 History, 在分布式中表现的更为明显, 因为每个副本都是一个独立的 History,
所以在讨论分布式前,我就开始以多重 History 的方式去理解问题.

让我们通过一个具体的例子来说明多重宇宙的概念：

假设有一个变量 x，两个线程 A 和 B 同时对其进行操作：

1. 在时刻 t0，线程 A 和 B 都读取了 x 的值，记为 x0
2. 在时刻 t1，线程 A 将 x 更新为 x1，形成 History `{t0->t1(x1)}`
3. 在时刻 t2，线程 B 尝试将 x 更新为 x2，形成 History `{t0->t2(x2)}`

在单宇宙模型中（即普通变量的实现），x 只能保存一个 History。
也就是说当 B 更新 x 时，2 个 History 必须舍弃掉一个,
现实中按照更新时间, B 会覆盖掉 A 的更新，导致 `{t0->t1(x1)}` 被 `{t0->t2(x2)}` 替代。

而在多重宇宙模型中（如我们的 Node API），两个 History 可以并存。
即 x 可以存储先后 2 次更新的 History,
B 的更新不会覆盖 A 的更新，而是与之共存。此时读取 x 的值会同时得到 `{t0->t1(x1)}` 和 `{t0->t2(x2)}`。

这个场景展示了经典的"更新丢失"问题。
从多重宇宙的视角来看，问题在于 B 的 History 与 A 的 History 不具有连续性，即 B 的 History 不是 A 的 History 的 successor。
这种非连续的 History 替换导致了信息丢失。

要解决"更新丢失"的问题, 方法是确保 B 的 History 成为 A 的 History 的 successor。
具体做法是：当 B 读取 x 的值后，要确保在 B 更新 x 之前，x 中的 History 没有产生新的事件（即不允许其他线程更新）。
否则就会产生分支宇宙，无法保证 B 的更新不会覆盖其他宇宙的 History。

![](multiverse-cas.x.svg)

我们通过原子更新这个简单例子，展示了多重宇宙如何帮助我们理解并发问题。
这种思维方式对于理解分布式环境下的并发问题尤其有帮助，因为分布式一致性本质上就是一个无锁的并发问题。
事实上，分布式一致性协议的核心就在于如何从多重宇宙中选择出一个确定的结果。

引入多重宇宙概念的一个重要原因是：当我们从物理时钟(wall clock)转向虚拟时间时，我们获得了自由操作时间的能力。
这种能力必然导致多个不可合并的 History 时间线的产生。可以说，一旦我们能够操纵时间，就不可避免地要面对多重宇宙的问题。

从多重宇宙的视角来看，分布式系统中的一致性问题和高可用(HA)复制策略反而变得更加清晰。
相比于现实世界中基于线性时间的分布式 HA，这种方法更接近问题的本质。
因此，我们将首先探讨多重宇宙中的 HA 实现，然后再研究如何将多重 History 合并为线性 History，最终理解 paxos 和 raft 这样的一致性协议。
