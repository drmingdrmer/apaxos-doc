# 论共识算法在高时间维度中与空间维度的转换以及狭义相对论的涌现

## 摘要

Raft 用单调递增的标量 `term` 作为逻辑时钟。标量 term 将所有领导权竞争压到一条线性时间轴上，因此协议可以用“更大 term 淘汰更小 term”这一规则消除旧 leader。本文研究一个理想化变体：把 Raft 的 `term` 替换为二维向量，并用逐坐标偏序定义 term 的大小。该替换把协议可见历史从一维线性时间提升为二维偏序时间。进一步地，当二维 term 被解释为光锥坐标时，逐坐标偏序与 1+1 维 Minkowski 时空的因果序同构；观察者选择某一方向作为自身时间轴时，剩余的反对称自由度自然被解释为空间；保持因果偏序与提交深度不变量的坐标变换正是 Lorentz boost。若多个不可比较的 leader 持续提交日志，并且以相同的不变提交深度比较它们的位置，则 leader 的二维 term 趋近于双曲线族。该双曲线在标准时空坐标中写作 \(t^2-x^2=\rho^2\)，即狭义相对论中的不变间隔曲线。

本文的结论不是“生产环境 Raft 自动产生物理相对论”。结论是一个形式化对应：在二维偏序 term、因果保持观察者、提交深度归一化这三个假设下，Raft 式共识历史的几何表示与 1+1 维狭义相对论具有同构结构。

## 关键词

Raft，共识算法，逻辑时钟，向量时钟，偏序，Minkowski 时空，Lorentz 变换，双曲线

## 1. 引言

Raft 将共识问题拆成 leader election、log replication 和 safety 三部分。`term` 是其中最关键的全局代数对象：它单调增加，随 RPC 传播，较小 term 的请求会被拒绝，candidate 或 leader 一旦发现更高 term 就退回 follower。Raft 论文明确把 term 称为 logical clock。这个设计给协议带来一个强约束：任意两个可见领导权时代都能线性比较。

线性比较是工程上有效的简化，但它也抹掉了分布式系统原本的偏序结构。Lamport 的 happened-before 关系本身只是偏序；Fidge 和 Mattern 的向量时钟说明，如果希望保留因果关系，就应把时间表示成向量，并用逐坐标偏序比较。Mattern 还指出，分布式虚拟时间与 Minkowski 相对论时空有紧密类比。本文沿着这条线索问一个更具体的问题：如果 Raft 的 term 不再是标量，而是二维向量，会出现怎样的几何？

核心观察很短：

\[
\tau=(\tau_1,\tau_2),\quad
\tau\preceq\sigma \Longleftrightarrow \tau_1\le\sigma_1 \land \tau_2\le\sigma_2
\]

这不是普通二维平面上的任意顺序，而是一个因果锥。令

\[
t=\frac{\tau_1+\tau_2}{2},\quad x=\frac{\tau_1-\tau_2}{2}
\]

则

\[
\tau_1=t+x,\quad \tau_2=t-x
\]

逐坐标偏序

\[
\Delta\tau_1\ge 0,\quad \Delta\tau_2\ge 0
\]

等价于

\[
\Delta t\ge |\Delta x|
\]

这正是 1+1 维 Minkowski 时空的未来因果锥。二维 term 的两个分量都可以被看成时间，但当观察者选择一个方向作为自身时间轴后，两个时间分量的和变成时间，差变成空间。空间不是额外加入的实体，而是从二维时间的相对差异中被解释出来的自由度。

本文要证明三件事：

1. 二维 term 的逐坐标偏序与 1+1 维 Minkowski 因果序同构。
2. 保持该偏序和提交深度不变量的观察者变换等价于 Lorentz boost。
3. 多个持续提交日志的 leader 在相同不变提交深度下位于同一双曲线；若提交深度只渐近相等，则它们渐近收敛到该双曲线族。

## 2. 相关工作

Raft 的原始设计使用标量 term。每个 server 维护 `currentTerm`，它随时间单调增加；RPC 携带 term；若收到更大的 term，server 更新本地 term；若请求携带过期 term，server 拒绝请求。多数派投票保证同一 term 中最多只有一个 leader。

Lamport 的逻辑时钟从 happened-before 偏序出发，构造能扩展该偏序的逻辑时间。关键点是：分布式事件之间不一定存在全局先后关系，强行线性化只是某种一致选择，不是事件结构本身。

Fidge 的时间戳数组和 Mattern 的虚拟时间进一步保留偏序本身。向量时钟以逐坐标最大值传播知识，使事件的可比性与潜在因果影响对应。Mattern 明确提出，线性时间结构并不总适合分布式系统，向量时间形成偏序格，并与 Minkowski 相对论时空存在类比。

狭义相对论中，Lorentz 变换保持时空间隔

\[
s^2=t^2-x^2
\]

不变。若使用光锥坐标

\[
u=t+x,\quad v=t-x
\]

则因果未来由 \(u\) 和 \(v\) 同时增加描述，boost 则表现为一个方向放大、另一个方向缩小的 squeeze map：

\[
u'=e^{-\eta}u,\quad v'=e^{\eta}v
\]

这与二维 term 的逐坐标偏序完全对齐。

## 3. 二维 term Raft 的理想化模型

设协议可见 term 空间为

\[
T=\mathbb{N}^2
\]

并定义逐坐标偏序

\[
a\preceq b \Longleftrightarrow a_1\le b_1 \land a_2\le b_2
\]

若 \(a\preceq b\) 且 \(a\ne b\)，记为 \(a\prec b\)。若既非 \(a\preceq b\)，也非 \(b\preceq a\)，则称 \(a\) 与 \(b\) 不可比较，记为 \(a\parallel b\)。

二维 term 版本的 Raft 不再只有“更大”和“更小”两种关系，而有三种协议状态：

1. \(m\prec currentTerm\)：消息过期，应拒绝。
2. \(currentTerm\prec m\)：消息来自因果未来，应更新本地 term 并退回 follower。
3. \(m\parallel currentTerm\)：消息来自并发方向，不能用标量 Raft 的规则直接判定谁淘汰谁。

第三种情况是本文几何的来源，也是工程风险的来源。若没有额外规则，多个不可比较 leader 可以同时存在。它们可以对应同一个二维时间锥中的不同方向，但这不再是原始 Raft 的安全模型。若要保留线性izable state machine，需要额外的冲突处理机制，例如对不可比较 leader 分支做确定性合并、要求命令可交换，或引入第二层序列化规则。本文讨论的是 term 几何与因果结构，不主张把该模型直接替换生产 Raft。

### 3.1 事件与 term 更新

令事件集合为 \(E\)，包括本地事件、发送 RPC、接收 RPC、赢得选举、追加日志、提交日志等。每个事件 \(e\in E\) 有一个二维时间戳 \(\tau(e)\in T\)。

本地单调性要求：

\[
e \to_l f \Longrightarrow \tau(e)\preceq\tau(f)
\]

其中 \(\to_l\) 表示同一节点上的本地先后关系。

消息传播要求：若事件 \(e\) 发送消息，事件 \(f\) 接收该消息，则

\[
\tau(e)\preceq\tau(f)
\]

典型更新规则为：

\[
currentTerm := \max(currentTerm, messageTerm)+\delta
\]

其中 \(\max\) 是逐坐标最大值，\(\delta\in\mathbb{N}^2\) 是非负增量，且在产生新协议事件时至少有一个分量为正。

### 3.2 维度限制

完整向量时钟通常需要与独立进程数相同的维度，才能无损表示任意分布式计算的 happen-before 关系。二维 term 只能无损表示 order dimension 不超过 2 的事件偏序，或表示协议选择暴露出来的两个因果方向。本文的形式化命题均建立在“协议可见历史被二维 term 定义”的模型上，而不是声称二维向量可以编码任意规模集群的全部因果关系。

## 4. 从二维时间到 Minkowski 时空

为了得到连续几何，先把 \(T=\mathbb{N}^2\) 嵌入正实数锥

\[
C=\mathbb{R}_{>0}^2
\]

离散模型只是该连续模型的格点采样。令

\[
\Phi:C\to M,\quad
\Phi(a,b)=\left(\frac{a+b}{2},\frac{a-b}{2}\right)=(t,x)
\]

其中 \(M\) 是 1+1 维时空平面。

### 定理 1：二维 term 偏序与未来因果锥同构

对任意 \(p=(a,b),q=(c,d)\in C\)，令

\[
\Phi(q)-\Phi(p)=(\Delta t,\Delta x)
\]

则

\[
p\preceq q \Longleftrightarrow \Delta t\ge |\Delta x|
\]

#### 证明

由定义：

\[
\Delta t=\frac{(c-a)+(d-b)}{2},\quad
\Delta x=\frac{(c-a)-(d-b)}{2}
\]

因此

\[
\Delta t+\Delta x=c-a,\quad
\Delta t-\Delta x=d-b
\]

\(p\preceq q\) 等价于

\[
c-a\ge 0,\quad d-b\ge 0
\]

也就是

\[
\Delta t+\Delta x\ge 0,\quad \Delta t-\Delta x\ge 0
\]

这等价于

\[
\Delta t\ge \Delta x,\quad \Delta t\ge -\Delta x
\]

因此

\[
\Delta t\ge |\Delta x|
\]

反向推理完全相同。定理得证。

该定理说明，二维 term 的“未来”不是整个右上方平面，而是在 \((t,x)\) 坐标中速度不超过 1 的因果未来。不可比较的 term 对应 spacelike separation：它们的先后不能由协议因果关系决定。

### 推论 1：不可比较 term 是空间样分离

若 \(p\parallel q\)，则 \(\Delta a\Delta b<0\)。在 \((t,x)\) 坐标中，

\[
\Delta s^2=(\Delta t)^2-(\Delta x)^2=\Delta a\Delta b<0
\]

因此 \(p\) 与 \(q\) 为空间样分离。不同观察者可以给它们分配不同的时间顺序，但没有观察者能把它们变成因果可达关系。

#### 证明

不可比较意味着一个坐标增加而另一个坐标减少，因此 \(\Delta a\Delta b<0\)。又因为

\[
\Delta t=\frac{\Delta a+\Delta b}{2},\quad
\Delta x=\frac{\Delta a-\Delta b}{2}
\]

所以

\[
(\Delta t)^2-(\Delta x)^2
=\frac{(\Delta a+\Delta b)^2-(\Delta a-\Delta b)^2}{4}
=\Delta a\Delta b<0
\]

推论得证。

## 5. 观察者、时间轴与空间轴

二维 term 的两个分量 \(a,b\) 都是单调时间。观察者不是在其中任选一个分量作为时间，而是选择一条因果方向作为自身静止方向。设观察者的 rapidity 为 \(\eta\)，定义变换

\[
a'=e^{-\eta}a,\quad b'=e^{\eta}b
\]

再定义

\[
t'=\frac{a'+b'}{2},\quad x'=\frac{a'-b'}{2}
\]

代入 \(a=t+x,\ b=t-x\)，得到

\[
t'=\cosh\eta\ t-\sinh\eta\ x
\]

\[
x'=-\sinh\eta\ t+\cosh\eta\ x
\]

令 \(v=\tanh\eta\)，\(\gamma=\cosh\eta=1/\sqrt{1-v^2}\)，则

\[
t'=\gamma(t-vx),\quad x'=\gamma(x-vt)
\]

这就是 1+1 维 Lorentz boost，取 \(c=1\)。

### 定理 2：保持提交深度不变量的观察者变换是 Lorentz boost

设观察者变换在光锥坐标中只改变尺度：

\[
F_{\alpha,\beta}(a,b)=(\alpha a,\beta b),\quad \alpha,\beta>0
\]

若它保持不变量

\[
\rho^2=ab
\]

则存在唯一 \(\eta\in\mathbb{R}\)，使得

\[
F_{\alpha,\beta}(a,b)=(e^{-\eta}a,e^{\eta}b)
\]

并且它在 \((t,x)\) 坐标中是 Lorentz boost。

#### 证明

保持 \(\rho^2=ab\) 要求：

\[
(\alpha a)(\beta b)=ab
\]

对所有 \(a,b>0\) 成立，因此

\[
\alpha\beta=1
\]

令 \(\alpha=e^{-\eta}\)，则 \(\beta=e^\eta\)，其中

\[
\eta=-\ln\alpha=\ln\beta
\]

唯一。代入

\[
a'=e^{-\eta}(t+x),\quad b'=e^{\eta}(t-x)
\]

得到

\[
t'=\frac{a'+b'}{2}
=\frac{e^{-\eta}(t+x)+e^{\eta}(t-x)}{2}
=\cosh\eta\ t-\sinh\eta\ x
\]

\[
x'=\frac{a'-b'}{2}
=\frac{e^{-\eta}(t+x)-e^{\eta}(t-x)}{2}
=-\sinh\eta\ t+\cosh\eta\ x
\]

这正是 Lorentz boost。定理得证。

该定理给出“从二维时间中涌现空间”的精确定义。观察者自己的时间轴是 \(x'=0\)，即

\[
a'=b'
\]

换回原坐标：

\[
\frac{a}{b}=e^{2\eta}
\]

这是一条穿过原点的因果方向。与该方向 Minkowski 正交的自由度是 \(x'\)，它被解释为空间坐标。这里的“正交”不是欧几里得正交，而是由不变量 \(t^2-x^2\) 定义的伪欧几里得正交。

## 6. 多 leader 提交与双曲线

现在讨论多个 leader 同时存在的情况。令第 \(i\) 个 leader 在提交深度 \(k\) 时的二维 term 位置为

\[
P_i(k)=(a_i(k),b_i(k))\in C
\]

定义不变提交深度：

\[
\rho(P)=\sqrt{ab}
\]

在时空坐标中，

\[
\rho(P)^2=ab=t^2-x^2
\]

因此固定提交深度 \(\rho\) 的点集为

\[
H_\rho=\{(a,b)\in C:ab=\rho^2\}
\]

映射到 \((t,x)\) 后为

\[
t^2-x^2=\rho^2,\quad t>0
\]

这是一族双曲线。

### 6.1 为什么提交深度应取几何平均

标量 Raft 中，commit index 是线性数轴上的进度。二维 term 中，若用 \(a+b\) 作为进度，不同观察者会改变该值；若用 \(a-b\) 作为进度，它甚至可能为负。乘积 \(ab\) 是自然候选，因为它在 squeeze 变换

\[
(a,b)\mapsto(e^{-\eta}a,e^\eta b)
\]

下不变。于是 \(\rho=\sqrt{ab}\) 是所有观察者共享的逻辑提交深度，对应相对论中的 proper time。

### 定理 3：等提交深度的 leader 位于同一双曲线

若所有 leader 在比较时满足

\[
\rho(P_i(k))=k
\]

则所有 \(P_i(k)\) 均位于同一双曲线

\[
H_k=\{(a,b):ab=k^2\}
\]

在 \((t,x)\) 坐标中等价于

\[
t^2-x^2=k^2
\]

#### 证明

由 \(\rho(P_i(k))=k\) 和 \(\rho(P)^2=ab\)，直接得到

\[
a_i(k)b_i(k)=k^2
\]

所以 \(P_i(k)\in H_k\)。再由坐标变换 \(a=t+x,\ b=t-x\)，得到

\[
ab=(t+x)(t-x)=t^2-x^2
\]

因此所有 leader 在标准时空坐标中位于 \(t^2-x^2=k^2\)。定理得证。

### 定理 4：渐近等提交深度导致渐近双曲线

设每个 leader 的提交深度满足

\[
\rho(P_i(k))=k+\epsilon_i(k),\quad \epsilon_i(k)=o(k)
\]

并且 leader 方向有界，即存在常数 \(0<m<M<\infty\)，对所有足够大的 \(k\) 都有

\[
m\le \frac{a_i(k)}{b_i(k)}\le M
\]

则 \(P_i(k)\) 到双曲线 \(H_k\) 的相对距离趋于 0：

\[
\frac{\operatorname{dist}(P_i(k),H_k)}{k}\to 0
\]

#### 证明

记

\[
P_i(k)=(a,b),\quad \rho=\sqrt{ab}=k+\epsilon_i(k)
\]

构造同一射线上的点

\[
Q=\frac{k}{\rho}P_i(k)=\left(\frac{k}{\rho}a,\frac{k}{\rho}b\right)
\]

则

\[
Q_1Q_2=\left(\frac{k}{\rho}\right)^2ab
=\left(\frac{k}{\rho}\right)^2\rho^2
=k^2
\]

所以 \(Q\in H_k\)。因此

\[
\operatorname{dist}(P_i(k),H_k)\le \|P_i(k)-Q\|
=\left|1-\frac{k}{\rho}\right|\|P_i(k)\|
\]

由 \(\rho=k+o(k)\)，可得

\[
\left|1-\frac{k}{\rho}\right|=o(1)
\]

方向有界意味着 \(a/b\) 不趋近 0 或无穷。由于 \(ab=\rho^2=O(k^2)\)，可得

\[
\|P_i(k)\|=O(k)
\]

于是

\[
\operatorname{dist}(P_i(k),H_k)=o(k)
\]

即

\[
\frac{\operatorname{dist}(P_i(k),H_k)}{k}\to 0
\]

定理得证。

该定理形式化了“多个 leader 不停提交日志时，leader 所在位置趋近于双曲线形状”。它依赖两个条件：提交深度渐近一致，且 leader 方向不退化。如果不同 leader 的提交速度相差线性量级，或者某个方向无限贴近光锥边界，则不能推出同一双曲线收敛。

## 7. 对共识语义的解释

在标量 Raft 中，leader 是线性时间上的唯一最大者。二维 term 改变了这件事：不可比较 leader 不是“谁更新谁旧”的关系，而是空间样分离的关系。一个 leader 沿 \(a/b=e^{2\eta_1}\) 的方向前进，另一个沿 \(a/b=e^{2\eta_2}\) 的方向前进，它们可以在某个观察者坐标中同时，也可以在另一个观察者坐标中一前一后，但这个顺序不属于协议因果事实。

这解释了为什么空间会从高维时间中出现。二维 term 原本只有两个时间分量：

\[
a=t+x,\quad b=t-x
\]

观察者只能访问保持因果结构的组合。对他而言，共同增长部分

\[
t=\frac{a+b}{2}
\]

是时间；差异部分

\[
x=\frac{a-b}{2}
\]

是空间。空间就是两个时间坐标之间无法被因果序消去的相对位移。

从协议角度看，commit 操作不断推动 leader 在正锥中前进。若所有 leader 的提交被同一个不变量 \(\rho=\sqrt{ab}\) 归一化，那么不同方向的 leader 不是排成直线，而是分布在

\[
ab=\rho^2
\]

上。该曲线在 \(t,x\) 中就是

\[
t^2-x^2=\rho^2
\]

这正是相对论中相同 proper time 的轨迹集合。Lorentz boost 改变观察者的时间轴和空间轴，但不改变这条双曲线。

## 8. 安全性边界

二维 term 不是原始 Raft 的无痛替换。原始 Raft 的安全性依赖同一 term 最多一个 leader，以及未来 leader 包含过去已提交日志。若 term 变成偏序，下面的情况会出现：

1. 两个 candidate 拥有不可比较 term。
2. 两者可能各自从不同多数派配置或分区中获得足够支持。
3. 两者提交同一 log index 的不同命令。
4. 若状态机命令不可交换，线性一致性被破坏。

因此，一个真正可实现的二维 term 共识协议必须增加至少一种机制：

1. 对不可比较 term 引入确定性 total order，回到标量化路径。
2. 允许多分支日志，但只对可交换命令或 CRDT 状态提交。
3. 把二维 term 仅作为因果元数据，真正的提交仍由单独的共识层序列化。
4. 使用 quorum certificate 证明某个 \(\rho\) 深度已被全局接受，再合并空间样分支。

如果不增加这些机制，本文的几何模型仍然成立，但它描述的是并发历史，不是安全的单日志共识。

## 9. 结论

把 Raft 的标量 term 替换为二维向量，并用逐坐标偏序比较大小，会把协议可见历史从线性时间提升为二维偏序时间。该二维时间在光锥坐标下与 1+1 维 Minkowski 因果结构同构。观察者选择某个因果方向作为自身时间轴后，两个时间分量的差异自然成为空间坐标。保持因果偏序与提交深度不变量的坐标变换就是 Lorentz boost。

在此模型下，多 leader 持续提交日志时，若以共同的不变提交深度 \(\rho=\sqrt{ab}\) 比较它们的位置，则 leader 分布在 \(ab=\rho^2\) 上；在标准时空坐标中，这就是 \(t^2-x^2=\rho^2\) 的双曲线。若提交深度只渐近一致，并且 leader 方向不退化，则它们相对双曲线的距离渐近为 0。

因此，狭义相对论的坐标变换并非凭空加入，而是由三个代数条件推出：二维偏序时间、因果保持观察者、乘积型提交不变量。工程上，这个模型不能直接替代 Raft；理论上，它给出了一条清晰路径：空间可以被看作高维逻辑时间在观察者选择下留下的不可比较自由度。

## 参考文献

1. Diego Ongaro, John Ousterhout. “In Search of an Understandable Consensus Algorithm.” USENIX ATC 2014, extended version. <https://web.stanford.edu/~ouster/cgi-bin/papers/raft-extended.pdf>
2. Leslie Lamport. “Time, Clocks, and the Ordering of Events in a Distributed System.” Communications of the ACM, 1978. <https://lamport.org/pubs/time-clocks.pdf>
3. Colin J. Fidge. “Timestamps in Message-Passing Systems That Preserve the Partial Ordering.” Australian Computer Science Communications, 1988. <https://ics.uci.edu/~cs230/reading/1.pdf>
4. Friedemann Mattern. “Virtual Time and Global States of Distributed Systems.” Proc. Workshop on Parallel and Distributed Algorithms, 1989. <https://pages.cs.wisc.edu/~ra/Classes/739-sp20/papers/mattern89.pdf>
5. ETH Zurich Distributed Systems Group. BibTeX entry for Mattern, “Virtual Time and Global States of Distributed Systems.” <https://vs.inf.ethz.ch/publ/bibtex.html?file=papers%2FVirtTimeGlobStates>
6. Albert Einstein. “On the Electrodynamics of Moving Bodies.” English translation, Wikisource 1920 edition. <https://en.wikisource.org/wiki/On_The_Electrodynamics_of_Moving_Bodies_%281920_edition%29>
7. Encyclopedia of Mathematics. “Lorentz transformation.” <https://encyclopediaofmath.org/wiki/Lorentz_transformation>
8. Wikipedia. “Light-cone coordinates.” <https://en.wikipedia.org/wiki/Light-cone_coordinates>
