### History 的相等比较

2个History相等, 当且仅当它们的时间DAG的结构相同, 即它们有相同的时间节点集合(vertex), 相同的时间关系(edge) 和相同的时间节点上的事件(Event).

由于所有时间 T 都是 Unique 的, 这简化了对 History 的比较: 对于2个 History 上的某个时间 T, 这个时间点上只可能有 Event 或没有 Event, 而不会有不同的 Event。因此比较 History 时, 只需检查每个时间点上是否有 Event。

例如, 所有时间点都相同, 时间顶点上的Event也想通, 但依赖关系不同也不认为是相等的History, 因为左边T=2和T=3没有先后关系, 可以以任意顺序解释; 右边T=2必须在T=3之前处理.

![](history-eq.x.svg)


在分布式一致性协议中, 实际上我们只需要关注时间DAG的结构, 而不需要关注节点上具体的Event内容。
分布式一致性算法的目标是在多个副本上保持所有副本上的写入顺序一致。
