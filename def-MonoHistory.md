一个 History 有一个或多个**最大时间**, 也就是 DAG 中没有出向边的顶点: `{T_1, T_2, ...}`.
显然 History 中,的多个**最大时间** 之间是不可比较大小的(因为表示时间的 DAG 中有边才代表有大小关系).

如果一个 History 中只有一个**最大时刻**, 那么它是一个单一的历史, 即 `MonoHistory`, 后面也会用`HomoHistory(t)` 的形式来表示这个单一历史的最大时间是t, 我们称之为 t 时刻的 History. 即所有能通过一些路径到达 t 的 Event 的顶点的集合.

> 我们现实世界中, wall clock 是线性的时间的, 因此一个 History 一定是一个单向链, 只有一个最大时刻, 就是墙上时钟的当前时刻.


#TODO: 加图