# 分布式系统

我们有了一个系统 State 的描述方式 History, 接下来在分布式环境中把它实现高可用,
即通过多个 History 的副本来实现高可用.


我们先来看 History 部分.

一个在分布式系统, 可以简单的看作多个节点的集合

```rust
struct Distributed {
    nodes: BTreeMap<NodeId, Node>,
}
```

Node提供History的读写接口, 参考 [[../../../../pieces/todo]]. 现在我们在这个Distributed 系统上建立高可用的存储.
这个系统是通过多个Node的副本来提供一定的故障的冗余的.

## 分布式中的副本定义

副本这个概念, 在分布式领域, 不仅包括数据内容的一致, 也包括数据变更发生的时间一致.

在 [[desc-Time-based-RW]] 中我们展示了读写接口隐含的时间属性：每次写入的数据都带有写操作发生的时间。
这是分布式系统复杂性的根源：因为不同节点的本地时间不同, 我们无法用单机提供的接口在每个节点上写入完全相同的副本, 因为每个副本的写入都带有写入时间的信息, 即使写入的数据一样, 写入时间的差异也会最终导致每个节点的状态不一致。

这导致节点之间的数据不完全对等。这种不对等并不体现在数据本身，但最终是会体现在"读"操作的结果上。

例如在下面的例子中，虽然 N1 和 N2 写入了相同的数据，但由于写操作发生的时间不同，导致读操作的结果不一致。本质上，是因为发生在 N1 和 N2 上的写操作具有不同的时间属性，最终导致了结果的不一致。

```
N3 |
N2 | write(1)   write(2)   read() -> 2
N1 | write(2)   write(1)   read() -> 1
-----+----------+----------+--------------> time
```

因此，我们需要引入虚拟时间来替代物理时钟。这是因为我们无法控制物理时钟，但可以完全控制虚拟时间。
通过虚拟时间，我们可以实现真正意义上的"多副本"系统——不仅数据本身保持一致，数据变更发生的（虚拟）时间也保持一致。