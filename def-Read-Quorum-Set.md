## Read Quorum Set

对一个提供了某些HA指标的分布式系统,
都显式的或隐含的定义了可用于 `read()` 的 `node_set` 有哪些, 
也就是HA保证通过哪些节点的读才能得到系统保证的结果, 使用其他node set读取到的结果不保证正确.

- 例如单机系统, `read()` 可用的 `node_set` 就是唯一这个节点`{{1}}`,
  显然用一个空的 `node_set` 去读是没意义的.

- 一个不做任何限制的 3 副本系统中,
  `read()`可用的`node_set`是所有非空节点集合: `{{1}, {2}, {3}, {1,2}, {2,3}, {1,3}, {1,2,3}}`
  这样一个系统中`read()`是没有任何高可用保证的, 因为它暗示了一个写操作必须写到每一个 node 上才能被合法的读读到.

- 一个 [多数派读写][] 的 3 节点系统中(n=3, w=2, r=2; 写操作必须写到2个节点才认为完成, 读操作必须读2个节点),
  `read()`可用的`node_set`是至少包含 2 节点的集合: `{{1,2}, {2,3}, {1,3}, {1,2,3}}`,


**def-Read-Quorum-Set**
**def-Read-Quorum**:


这个系统HA承诺的可用于读的`node_set`的集合, 称之为`read_quorum_set`.
`read_quorum_set` 中的一个元素是一个节点集合, 称之为一个`read_quorum`.
例如3部分多数派读写的系统中, `read_quorum_set` 是所有可能的2个节点的集合: `{{1,2}, {2,3}, {1,3}}`(也包括所有3个节点的集合).
其中`{1,2}`就是一个`read_quorum`.


#TODO
考虑去掉基于时间T的read:

在某个时刻`T`, 可合法的用于`read()`的`node_set`的集合, 就是系统在这个`T`时刻的`read_quorum_set(T)`, 注意不同时间T的`read_quorum_set(T)`可能不同, 例如 [Raft][] 的配置变更时.




[多数派读写]: http://a
[Raft]: http://a